// Re-exports for convenience. The core decode API lives in `context.rs`
// (CodecContext::send_packet / receive_frame) and `codec.rs` (trait Decoder).
//
// This module will hold decode-specific helpers as they become needed.
