// Re-exports for convenience. The core encode API lives in `context.rs`
// (CodecContext::send_frame / receive_packet) and `codec.rs` (trait Encoder).
//
// This module will hold encode-specific helpers as they become needed.
